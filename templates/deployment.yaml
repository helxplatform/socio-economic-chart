apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "social.fullname" . }}
  labels:
    {{- include "social.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "social.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "social.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "social.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            {{ if .Values.connexion.server }}
            - name: CONNEXION_SERVER
              valueFrom:
                secretKeyRef:
                  key: CONNEXION_SERVER
                  name: {{ include "social.fullname" . }}-env
            {{- end }}
            {{ if .Values.connexion.debug }}
            - name: CONNEXION_DEBUG
              valueFrom:
                secretKeyRef:
                  key: CONNEXION_DEBUG
                  name: {{ include "social.fullname" . }}-env
            {{- end }}
            {{ if .Values.connexion.port }}
            - name: CONNEXION_PORT
              valueFrom:
                secretKeyRef:
                  key: CONNEXION_PORT
                  name: {{ include "social.fullname" . }}-env
            {{- end }}
            {{ if .Values.connexion.keyfile }}
            - name: CONNEXION_KEY_FILE
              valueFrom:
                secretKeyRef:
                  key: CONNEXION_KEY_FILE
                  name: {{ include "social.fullname" . }}-env
            {{- end }}
            {{ if .Values.connexion.certfile }}
            - name: CONNEXION_CERT_FILE
              valueFrom:
                secretKeyRef:
                  key: CONNEXION_CERT_FILE
                  name: {{ include "social.fullname" . }}-env
            {{- end }}
            {{ if .Values.sysPath.exposures }}
            - name: EXPOSURES
              valueFrom:
                secretKeyRef:
                  key: EXPOSURES
                  name: {{ include "social.fullname" . }}-env
            {{- end }}
            {{ if .Values.sysPath.controllers }}
            - name: CONTROLLERS
              valueFrom:
                secretKeyRef:
                  key: CONTROLLERS
                  name: {{ include "social.fullname" . }}-env
            {{- end }}
            {{ if .Values.postgres.host }}
            - name: POSTGRES_HOST
              valueFrom:
                secretKeyRef:
                  key: POSTGRES_HOST
                  name: {{ include "social.fullname" . }}-env
            {{- end }}
            {{ if .Values.postgres.port }}
            - name: POSTGRES_PORT
              valueFrom:
                secretKeyRef:
                  key: POSTGRES_PORT
                  name: {{ include "social.fullname" . }}-env
            {{- end }}
            {{ if .Values.postgres.database }}
            - name: POSTGRES_DATABASE
              valueFrom:
                secretKeyRef:
                  key: POSTGRES_DATABASE
                  name: {{ include "social.fullname" . }}-env
            {{- end }}
            {{ if .Values.postgres.username }}
            - name: POSTGRES_USERNAME
              valueFrom:
                secretKeyRef:
                  key: POSTGRES_USERNAME
                  name: {{ include "social.fullname" . }}-env
            {{- end }}
            {{ if .Values.postgres.password }}
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: POSTGRES_PASSWORD
                  name: {{ include "social.fullname" . }}-env
            {{- end }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /socio_economic__api/v1/swagger.json
              port: 8080
          readinessProbe:
            httpGet:
              path: /socio_economic_api/v1/ui/
              port: 8080
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
